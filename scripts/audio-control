#!/usr/bin/env bash

set -euo pipefail

# Open pavucontrol on the output-device configuration page.
if pgrep -x pavucontrol >/dev/null; then
    hyprctl dispatch focuswindow "class:^(pavucontrol)$"
else
    pavucontrol --tab=5 &
fi
